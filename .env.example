# API base URL (backend)
VITE_API_URL=http://localhost:3000

# Optional dev-only admin API key (if you don't want to login yet)
# If set, requests will use x-api-key when no JWT token present
# VITE_ADMIN_API_KEY=your-admin-key-here
